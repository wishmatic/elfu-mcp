# MCP/Internals (optional)

HOST=0.0.0.0
PORT=8080

LOG_LEVEL=info # debug, info, warn, error, dpanic, panic, fatal

# Auth

API_KEY=change-me

# Files

PUBLIC_HOST=
FILES_DIR=files # "files" resolves to /data/files
